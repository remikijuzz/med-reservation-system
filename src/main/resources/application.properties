# ------------------------
# H2 (development) / JPA
# ------------------------

# H2 (in-memory)
spring.datasource.url=jdbc:h2:mem:meddb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update

# Wyłącz domyślny, generowany login/password Spring Security
spring.security.user.name=
spring.security.user.password=

# Logowanie SQL (opcjonalne)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.h2.console.enabled=true


# ------------------------
# Port aplikacji (opcjonalnie zmień, jeśli nie 8080)
# ------------------------
# server.port=8080
